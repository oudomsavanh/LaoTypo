rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Public reads allowed for gameplay, writes restricted to Cloud Functions / service accounts
    match /gameWords/{documentId} {
      allow get, list: if true;
      allow create, update, delete: if false;
    }
  }
}
